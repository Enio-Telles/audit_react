1. Criar a estrutura de diretórios em `server/python/audit_engine/tabelas/` para as 11 tabelas:
    - produtos_unidades
    - produtos
    - produtos_agrupados
    - fatores_conversao
    - produtos_final
    - nfe_entrada
    - mov_estoque
    - aba_mensal
    - aba_anual
    - id_agrupados
    - produtos_selecionados
2. Para cada tabela:
    - Criar `__init__.py` (vazio)
    - Criar `contrato.py` (movendo a definição de `contratos/tabelas.py`)
    - Criar `gerador.py` (movendo o código dos arquivos em `modulos/`)
3. Atualizar `contratos/base.py` (o atual `contratos/tabelas.py`) para conter apenas as classes base e lógicas de registro.
4. Remover a pasta `modulos/` que ficará obsoleta.
5. Criar `tabelas/__init__.py` para importar todas as tabelas.
6. Atualizar as importações de contratos em `gerador.py` e `api.py`.
7. Implementar o stub do gerador `nfe_entrada` em `tabelas/nfe_entrada/gerador.py` usando nomes de funções em português e comentários.
8. Implementar o stub do gerador `mov_estoque` em `tabelas/mov_estoque/gerador.py` usando nomes de funções em português e comentários.
9. Implementar o stub do gerador `produtos_unidades` em `tabelas/produtos_unidades/gerador.py`.
10. Adicionar arquivos `AGENTS.md` em cada subpasta com instruções específicas, se necessário.
