{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for desktop and mobile shells.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default",
    "shell:default"
  ]
}
